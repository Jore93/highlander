import axios from 'axios';
import dotenv from 'dotenv';
dotenv.config();

export const fetchParagraphs = (state: any, action: any) => {
  return (dispatch: any) => {
    console.log('paragraph');
  }
}
